import React from "react";

const AllPuppies = (props) => {
  const myPuppies = props.puppyData;

  return <div className="box"> {`This is the AllPuppies Component`}</div>;
};

export default AllPuppies;
